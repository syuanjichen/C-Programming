/*Day 42, calculate the size of an input string*/
#include <stdio.h>
#include <stdlib.h>

int main()
{
    char str[]="Hello, C language";
    printf("The size of the string is %d bytes.\n",sizeof(str));
    system("pause");
    return 0;
}